 

window.onload = _ =>{
    console.log('estou funcionando')
    // setTimeout(_=>{
    let window_height = window.innerHeight
    let header_height = document.getElementById('nav').offsetHeight
    let herocontainer = document.getElementById('herocontainer')
    let cardswrapper = document.getElementById('cardswrapper')

    // herocontainer.style.height = `${(window_height - header_height) * 0.5}px`
    // cardswrapper.style.height = `${(window_height - header_height) * 0.5}px`

    // },10)
    
    // console.log(window_height, header_height);
    
    
}